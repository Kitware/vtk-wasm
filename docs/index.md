---
# `layout: home` with no `hero`/`features` frontmatter renders the page content
# full-bleed, which is what the custom landing needs. See
# .vitepress/theme/components/Landing.vue.
layout: home
title: VTK.wasm
description: Run the real VTK in a browser tab — C++ rendering and filtering compiled to WebAssembly.
---

<Landing />
